<div class="cont">
    <h4>Modificar la novedades</h4>
    <form action="/admin/adminMenu/modificar" method="post" enctype="multipart/form-data">
        <input type="hidden" value="{{novedad.id}}" name="id">

        <div class="form-group">
            <label for="">Titulo</label>
            <input type="text" class="form-control" name="titulo" value="{{novedad.titulo}}">
        </div>

        <div class="form-group">
            <label for="">subtitulo</label>
            <input type="text" class="form-control" name="subtitulo" value="{{novedad.subtitulo}}">
        </div>

        <div class="form-group">
            <p>
                <label>Imagen</label><input type="file" class="form-control" name="imagen">
            </p>
            <p>
                <label><input type="checkbox" name="img_delete" value="1">eliminar imagen actual</label>
                <input type="hidden" name="img_original" value="{{novedad.img_id}}">
            </p>
        </div>

        <div class="form-group">
            <label for="">Cuerpo</label>
            <textarea name="cuerpo" class="form-control">{{novedad.cuerpo}}</textarea>
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-primary">Modificar</button>
            {{#if error}}
            <p>{{message}}</p>
            {{/if}}
        </div>
    </form>
</div>